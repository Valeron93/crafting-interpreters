let a = 100;
{   
    let b = 1000;
    print(b);
}
print(a);


let b = 100;

fn create_adder(add) {
    fn inner(x) {
        return x + add;
    }

    return inner;
}

let adder = create_adder(10);

adder(10);